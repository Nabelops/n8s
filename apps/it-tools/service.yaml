apiVersion: v1
kind: Service
metadata:
  name: it-tools-service
  namespace: it-tools
spec:
  selector:
    app: it-tools
  ports:
    - protocol: TCP
      name: http
      port: 80
      targetPort: 80
