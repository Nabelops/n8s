apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - sealed-secret.yaml
  - oidc-sealed-secret.yaml
  - tailscale-sealed-secret.yaml
  - namespace.yaml
  - postgres.yaml
  - pvc.yaml
  - service.yaml
  - deployment.yaml
  - ingress.yaml

commonLabels:
  app: forgejo
