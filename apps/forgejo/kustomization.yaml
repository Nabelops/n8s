apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - postgres.yaml
  - pvc.yaml
  - service.yaml
  - deployment.yaml
  - ingress.yaml

commonLabels:
  app: forgejo
