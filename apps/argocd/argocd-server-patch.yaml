apiVersion: apps/v1
kind: Deployment
metadata:
  name: argocd-server
  namespace: argocd
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: argocd-server
  template:
    spec:
      hostAliases:
        - ip: 10.43.45.140
          hostnames:
            - auth.tailf95ba8.ts.net
      containers:
        - name: argocd-server
