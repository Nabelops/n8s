# This file is empty because the hostAliases patch is no longer needed 
# after CoreDNS is configured to forward .ts.net to Tailscale.
# Keeping the file as empty or removing the patch from kustomization.yaml
[]
