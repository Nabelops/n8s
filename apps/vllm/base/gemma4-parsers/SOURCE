These files are vendored from vllm-project/vllm and loaded into the running
vLLM container via a ConfigMap, because the official vllm-openai-rocm:gemma4
image ships without them even though the Gemma 4 recipe references them.

Pinned commit: f00c5539d7265719c188a5335159f68178f66e2f
Fetched on:    2026-04-12

Files and their upstream paths:
  tool_chat_template_gemma4.jinja  -> examples/tool_chat_template_gemma4.jinja
  gemma4_tool_parser.py            -> vllm/tool_parsers/gemma4_tool_parser.py
  gemma4_reasoning_parser.py       -> vllm/reasoning/gemma4_reasoning_parser.py
  gemma4_utils.py                  -> vllm/reasoning/gemma4_utils.py
  basic_parsers.py                 -> vllm/reasoning/basic_parsers.py

To refresh: re-run the curl block from serve.sh against a newer pinned SHA,
drop the files here, update this SOURCE, and commit. Do NOT re-point serve.sh
at raw.githubusercontent.com/.../main — the whole point of vendoring is to
make pod restarts independent of upstream.
